function submitForm() {
    const selectedPet = document.querySelector('input[name="game"]:checked').value;
    console.log(selectedPet);
    // You can do something with the selected pet here
  }